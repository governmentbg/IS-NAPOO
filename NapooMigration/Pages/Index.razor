@page "/sppoo"
@using System.Text

<PageTitle>Index</PageTitle>

<h1>Hello, world!</h1>

Welcome to your new app.

<button @onclick="test"> test </button>

@code {
    public void test()
    {
        var utf8Decoder = Encoding.UTF8.GetDecoder();


        var name = "1967854_ÐÐµÐ¾ÑÐ³Ð¸ ÐÐ¾Ð²Ð°ÐºÐ¾Ð² - CV.pdf";

        var bytes = Encoding.UTF8.GetBytes(name);

        var encoded = Encoding.Convert(Encoding.UTF8, Encoding.GetEncoding("ISO-8859-1"), bytes);

        string sConvertedString = Encoding.UTF8.GetString(encoded);
    }
}